<style>
  /* One Rides Tema - Logo Hizalaması */
  /* Mobilde ortala */
  #header-logo-inner {
    justify-content: center !important;
    width: 100%;
  }
  
  /* Desktop'ta menü ile aynı hizada başlat */
  @media (min-width: 1024px) {
    /* Menü container'ının sol padding'ini al */
    #header-nav-menu {
      padding-left: 0 !important;
      margin-left: 0 !important;
    }
    
    /* Logo container'ını menü ile aynı hizada başlat */
    #header-logo {
      flex: 0 0 auto !important;
      width: auto !important;
      max-width: none !important;
      margin-left: 0 !important;
      padding-left: 0 !important;
      margin-right: 0 !important;
      padding-right: 0 !important;
    }
    #header-logo-inner {
      justify-content: flex-start !important;
      width: auto !important;
      margin-left: 0 !important;
      padding-left: 0 !important;
      margin-right: 0 !important;
      padding-right: 0 !important;
    }
    #header-logo a,
    #header-logo #site-logo {
      margin-left: 0 !important;
      padding-left: 0 !important;
      margin-right: 0 !important;
      padding-right: 0 !important;
      display: inline-block !important;
    }
    #header-logo img,
    #header-logo h1 {
      margin-left: 0 !important;
      padding-left: 0 !important;
    }
  }
  
  /* Breadcrumb Stilleri - One Rides Tema */
  #breadcrumbs {
    padding-top: 1rem !important;
    padding-bottom: 1rem !important;
    margin-top: 0 !important;
    margin-bottom: 0 !important;
  }
  
  /* Header ve nav'dan sonra TÜM boşlukları kaldır */
  header {
    margin-bottom: 0 !important;
    padding-bottom: 0 !important;
  }
  
  header nav {
    margin-bottom: 0 !important;
    padding-bottom: 0 !important;
  }
  
  header + *,
  header + * > *,
  nav + * {
    margin-top: 0 !important;
    padding-top: 0 !important;
  }
  
  /* Main content - separator'dan hemen sonra */
  main,
  .main-content,
  turbo-frame {
    margin-top: 0 !important;
    padding-top: 0 !important;
  }
  
  /* Page container'lar */
  .page-container:first-child {
    padding-top: 0 !important;
    margin-top: 0 !important;
  }
  
  #breadcrumbs ul {
    display: flex !important;
    flex-direction: row !important;
    align-items: center !important;
    gap: 0.5rem !important;
    margin: 0 !important;
    padding: 0 !important;
    list-style: none !important;
  }
  
  #breadcrumbs li,
  #breadcrumbs li *,
  #breadcrumbs li a,
  #breadcrumbs li span {
    font-family: 'BlenderPro', sans-serif !important;
    font-size: 16px !important;
    font-weight: 500 !important;
    line-height: 1.5 !important;
    color: #4B5563 !important; /* text-gray-600 */
    text-transform: none !important;
    letter-spacing: normal !important;
    margin: 0 !important;
    padding: 0 !important;
  }
  
  #breadcrumbs li a {
    text-decoration: none !important;
    transition: color 0.2s ease !important;
  }
  
  #breadcrumbs li a:hover {
    color: #111827 !important; /* text-gray-900 */
  }
  
  #breadcrumbs .breadcrumb-separator {
    color: #9CA3AF !important; /* text-gray-400 */
    display: flex !important;
    align-items: center !important;
    justify-content: center !important;
  }
  
  #breadcrumbs .breadcrumb-separator svg {
    width: 16px !important;
    height: 16px !important;
    flex-shrink: 0 !important;
  }
  
  #breadcrumbs .breadcrumb-item {
    display: flex !important;
    align-items: center !important;
  }
  
  /* Navigation - KTM Clean Design */
  .nav-item-wrapper {
    position: relative;
    display: inline-block;
  }
  
  /* Menü item stilleri */
  .nav-item-wrapper .header--nav-link {
    position: relative;
    padding: 14px 20px !important; /* Top/bottom: 10px + 4px = 14px */
    margin: 0 2px !important;
    background: transparent !important;
    color: #111827 !important; /* Siyah */
    font-weight: 600 !important;
    font-size: 16px !important;
    transition: background 0.2s ease !important;
    border: none !important;
    display: block !important;
  }
  
  /* Hover efekti - Sadece hafif gri arka plan */
  .nav-item-wrapper:hover .header--nav-link {
    background: rgba(0, 0, 0, 0.04) !important; /* Çok hafif gri */
  }
  
  /* Active state - Sade */
  .nav-item-wrapper .menu-item--active {
    color: #111827 !important;
    background: rgba(0, 0, 0, 0.05) !important;
  }
  
  .dropdown-menu {
    position: absolute;
    top: calc(100% + 3px); /* Separator'dan sonra başla (3px separator yüksekliği) */
    left: 0;
    min-width: 100vw;
    margin-left: calc(-1 * var(--nav-offset, 0px));
    margin-top: 0 !important;
    opacity: 0;
    visibility: hidden;
    pointer-events: none;
    transition: opacity 0.2s ease-in-out, visibility 0.2s ease-in-out;
    z-index: 50;
  }
  
  .nav-item-wrapper:hover .dropdown-menu {
    opacity: 1;
    visibility: visible;
    pointer-events: auto;
  }
  
  /* Grid Container */
  .dropdown-menu .grid {
    justify-items: start;
    gap: 1rem;
  }
  
  /* Kategori Item Stilleri */
  .category-item {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-decoration: none;
    transition: all 0.2s ease;
    width: 100%;
    max-width: 120px;
  }
  
  .category-item:hover {
    transform: translateY(-2px);
  }
  
  .category-image {
    width: 80px;
    height: 80px;
    border-radius: 0.5rem;
    overflow: hidden;
    background-color: #f9fafb;
    margin-bottom: 0.5rem;
    border: 2px solid transparent;
    transition: all 0.2s ease;
  }
  
  .category-item:hover .category-image {
    border-color: #ff6600; /* KTM turuncu */
  }
  
  /* Dropdown - Separator ile kesintisiz */
  .dropdown-menu {
    border-top: none !important; /* Separator zaten var, tekrar border eklemeye gerek yok */
    box-shadow: 0 10px 40px rgba(0, 0, 0, 0.12) !important;
    border-radius: 0 !important;
  }
  
  /* Dropdown içi beyaz arka plan */
  .dropdown-menu > div {
    background: white;
    padding-top: 1.5rem;
  }
  
  /* Kategori hover rengi - Turuncu */
  .category-item:hover .category-name {
    color: #ff6600 !important;
  }
  
  .category-image-inner {
    transition: transform 0.2s ease;
  }
  
  .category-item:hover .category-image-inner {
    transform: scale(1.1);
  }
  
  .category-placeholder {
    display: flex;
    align-items: center;
    justify-content: center;
  }
  
  .category-name {
    font-size: 0.75rem;
    text-align: center;
    color: #374151;
    font-weight: 500;
    line-height: 1.2;
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
    transition: color 0.2s ease;
  }
  
  .category-item:hover .category-name {
    color: #f97316;
  }
</style>
